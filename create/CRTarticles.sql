CREATE TABLE Articles (
    article_id INT PRIMARY KEY,
    title VARCHAR(255),
    author VARCHAR(255),
    publish_date DATE,
    news_source VARCHAR(255),
    url VARCHAR(255),
    image VARCHAR(255),
    description TEXT,
    topic VARCHAR(255)
);
